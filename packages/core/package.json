{
  "name": "@sunland-ai/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/sdk.ts",
      "import": "./src/sdk.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:contract": "vitest run src/sdk.contract.test.ts src/sdk.recovery.contract.test.ts src/sdk.api-surface.contract.test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.6.2",
    "vite": "^5.4.8",
    "vitest": "^2.1.1"
  }
}
